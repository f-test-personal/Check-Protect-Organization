name: GitHub Actions Demo
on: 
  push



jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job "
      - run: echo $GITHUB_REF
      # - run: echo ${$GITHUB_REF}]
      - run: echo ${GITHUB_REF}
      - run: echo "$GITHUB_REF"
      # - run: echo GITHUB_REF
      - run: echo ${{github.ref}}
