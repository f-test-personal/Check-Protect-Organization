name: GitHub Actions Demo
on: 
  # pull_request:
  push:

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello > world.txt
      - uses: actions/upload-artifact@v2
        with:
          name: hoge hoge
          path: build/libs/world.txt

