name: GitHub Actions Demo
on:
  push:
  workflow_dispatch:
  
env: 
  SECRET_HOGE: ${{secrets.HOGEHOGE_VALUE}} 
  TOKEN: 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
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: curl -X POST \
                  -H "Authorization:token ${{env.TOKEN}}" \
                  -H "Accept:application/vnd.github.v3+json" \
                  https://api.github.com/repos/f-test-personal/Check-repository-dispatch/dispatches \
                  -d '{"event_type":"hogehoge"}'

