name: GitHub Actions Demo
on: 
  push

env:
  HOGE1: "HOGE"
  HOGE2: "HOGEHOGE"


jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job "
      - run: echo $GITHUB_REF
      # - run: echo ${$GITHUB_REF}]
      - run: echo ${GITHUB_REF}
      - run: echo "$GITHUB_REF"
      # - run: echo GITHUB_REF
      - run: echo ${{github.ref}}
      - run: echo ${GITHUB_REF#refs/heads/}
      - run: echo $GITHUB_REF#refs/heads/
      - run: echo $GITHUB_REF#refs/heads/-$GITHUB_SHA
      - run: echo ${{env.HOGE1}}
      - run: echo ${env.HOGE1}
  Explore-GitHub-Actions2: 
    runs-on: ubuntu-latest
    if: $GITHUB_REF =~ ^refs/heads/feature-.*$
    steps:
      - run: echo "hoge"
      
    
